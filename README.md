# SafeWays Landing Page

A Vite + React landing page for SafeWays.

## Local development

1. Install dependencies: `npm install`
2. Run the app: `npm run dev`
3. Build for production: `npm run build`

## Notes

This project is a static marketing site and does not rely on external app runtime services.
